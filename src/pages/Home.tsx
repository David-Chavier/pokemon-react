import { Typography } from '@mui/material';
import React from 'react';

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <Typography>Home</Typography>
    </React.Fragment>
  );
};

export default Home;
